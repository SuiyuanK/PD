  
    For emulating the "Print instance " command , Use the following commands in the GUI :

    source print_instance.tcl
    print_instance -o filename
   
    Note : Please copy the print_instance.pl also in the RedHawk Run directory 
